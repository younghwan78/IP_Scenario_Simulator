[Project Specification] Android SoC Multimedia Architecture Simulator
1. Project Overview
목표: Android 기반 SoC의 Multimedia IP(Camera, Video, Display, GPU, NPU 등)들의 동작을 시뮬레이션하여 Performance, Latency, Power를 예측하는 Discrete Event Simulator 개발. 핵심 요구사항:

SimPy 기반의 Event-driven simulation 구현.

NetworkX를 활용한 DAG(Directed Acyclic Graph) 기반 Scenario 모델링.

OTF(On-The-Fly)와 M2M(Memory-to-Memory) 데이터 흐름의 차이점 반영.

MVC(Model-View-Controller) 패턴을 적용하여 확장성 확보.

2. Technical Stack
Language: Python 3.10+

Core Logic: simpy (Event processing), networkx (Scenario topology)

Data Handling: pandas, numpy, yaml (Configuration)

Visualization: matplotlib or plotly (Gantt chart generation)

Architecture: MVC Pattern

3. Class Architecture & Design (Model Layer)
AI는 다음 클래스 구조를 기반으로 구현해야 합니다.

3.1. Hardware Modeling (Hardware Graph)
하드웨어는 계층적(Hierarchical) 구조를 가집니다.

HWNode (Base Class)

name (str): Component identifier.

clock_freq (float): 동작 주파수 (Hz).

power_static (float): Leakage power (mW).

power_dynamic (float): Active power coefficient (mW).

utilization (float): 현재 사용량 (0.0 ~ 1.0).

IPNode (Inherits HWNode)

용도: ISP, Codec, DPU 등 Pixel 처리 중심 IP.

ppc (float): Pixel Per Clock (클럭당 처리 픽셀 수).

efficiency (float): Stall 등을 고려한 효율 (0.0 ~ 1.0).

Method: get_processing_time(num_pixels) -> pixels / (clock * ppc * efficiency)

ProcessorNode (Inherits HWNode)

용도: CPU, DSP, NPU 등 Instruction/Cycle 중심 IP.

cycles_per_op (float): 단위 작업당 소요 Cycle.

Method: get_processing_time(num_ops) -> (num_ops * cycles_per_op) / clock

BusNode / MemoryNode (Shared Resource)

simpy.Resource를 사용하여 Bandwidth Contention 모델링.

3.2. Scenario Modeling (Logical Graph)
시나리오는 HW 위에서 실행되는 Task들의 흐름입니다. networkx.DiGraph를 사용합니다.

Node Attributes (Task Definition):

task_id: Unique Task Name.

mapped_hw: 실행될 HWNode의 이름.

workload: { "pixels": 12MP, "ops": 500M } 등 작업량 정의.

Edge Attributes (Dependency):

type: 'M2M' (Memory based, Sequential) or 'OTF' (Direct connection, Pipelined).

buffer_size: (Optional) M2M일 경우 버퍼 크기.

4. Simulation Engine Logic (Controller Layer)
4.1. SoCSimulator Class
Member: simpy.Environment, hw_registry (Dict[name, HWNode]), scenario_graph (NetworkX DiGraph).

Dependency Resolution (핵심 로직):

networkx.topological_sort를 사용하여 실행 순서 결정하지 않음(SimPy 이벤트 의존).

대신 각 Task는 자신의 Predecessor Task들의 종료 Event를 기다림 (SimPy Event).

4.2. Connection Logic Details
Case 1: M2M (IP_A -> DRAM -> IP_B)

IP_B Start condition: IP_A End Event triggered.

Latency: Time(IP_A) + Time(IP_B)

Case 2: OTF (IP_A -> IP_B directly)

IP_A와 IP_B는 동시에 시작(Start Event 공유).

Throughput Limiter: 두 IP 중 더 느린 쪽(Bottleneck)의 처리 속도에 전체 파이프라인이 맞춰짐.

Simulation: 두 Task 프로세스를 동시에 띄우되, 완료 시점은 max(Time(IP_A), Time(IP_B))로 동기화.

5. Input/Output Data Specifications
5.1. Input (YAML Format)
테스트를 위한 설정 파일 구조입니다. 파서는 이 구조를 지원해야 합니다.

YAML
hardware:
  - name: "ISP_Top"
    type: "Subsystem"
    children:
      - {name: "ISP_FE", type: "IP", clock: 600M, ppc: 4}
      - {name: "ISP_BE", type: "IP", clock: 600M, ppc: 2}
  - name: "VENC"
    type: "IP"
    clock: 400M
    ppc: 1
  - name: "CPU_Cluster"
    type: "Processor"
    clock: 2.0G

scenarios:
  - name: "4K_Recording"
    tasks:
      - {id: "t_sensor", hw: "Sensor_Ext", pixels: 8294400} # 4K
      - {id: "t_isp_fe", hw: "ISP_FE", pixels: 8294400}
      - {id: "t_venc", hw: "VENC", pixels: 8294400}
    edges:
      - {src: "t_sensor", dst: "t_isp_fe", type: "OTF"}
      - {src: "t_isp_fe", dst: "t_venc", type: "M2M"}
5.2. Output (Artifacts)
Simulation Log (CSV): TaskID, HW, StartTime, EndTime, Duration, PowerConsumed

Gantt Chart (Image): Y축은 HW Name, X축은 Time.

6. Implementation Steps (Step-by-Step Instructions)
AI에게는 다음 단계별로 코드를 작성하고 검증하도록 지시합니다.

Phase 1: Foundation (HW Model & SimPy Setup)
HWNode, IPNode, ProcessorNode 클래스 정의.

간단한 SoCSimulator 클래스 생성.

Test: 하나의 IP를 생성하고 작업을 던졌을 때, Clock과 PPC에 맞춰 예상된 시간에 끝나는지 print로 확인.

Phase 2: Scenario Graph & M2M Simulation
networkx 그래프를 생성하고 Node/Edge 추가 기능 구현.

Predecessor가 끝난 후 Successor가 시작되는 M2M 로직만 먼저 구현.

Test: A(1초) -> B(2초) 연결 시, B가 1초에 시작해서 3초에 끝나는지 확인.

Phase 3: Advanced Flow (OTF Implementation)
Graph Edge 속성에 OTF 타입 처리 로직 추가.

OTF로 연결된 노드들을 찾아 Grouping 하거나 동기화하는 로직 구현.

Test: A(Fast) -> B(Slow)가 OTF 연결 시, A와 B가 동시에 시작하고, 둘 다 B의 속도에 맞춰 끝나는지 확인.

Phase 4: Loader & Visualization (MVC Complete)
YAML 파일 Loader 구현.

Simulation 결과를 DataFrame으로 수집하는 Monitor 모듈 구현.

plotly.express.timeline을 이용한 시각화 기능 추가.

7. Verification & Test Plan (Unit Tests)
작성된 코드는 다음 unittest를 통과해야 합니다.

Calculation Test: 600MHz, 4PPC IP에서 4K(3840x2160) 프레임 처리 시간은 약 3.456ms여야 함.

Pipeline Test (OTF): 100fps IP와 30fps IP가 OTF로 연결되면 전체 성능은 30fps여야 함.

Concurrency Test: 서로 다른 HW에 매핑된 두 개의 독립적인 시나리오는 시간상 겹쳐서(Parallel) 실행되어야 함.

Resource Contention: (Optional/Advanced) 동일 IP에 두 개의 Task가 동시 요청되면 Queueing 되어 순차 실행되어야 함.